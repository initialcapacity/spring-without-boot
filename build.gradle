subprojects {
    defaultTasks "clean", "build"

    buildscript {
        repositories {
            mavenCentral()
        }
    }

    repositories {
        mavenCentral()
    }

    ext {
        spring_version = "4.1.8.RELEASE"
        kotlin_version = '1.0.0-rc-1036'
    }
}